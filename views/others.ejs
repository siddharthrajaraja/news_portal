<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title id="title" value=""></title>
</head>
<body>
    <%include partials/navbar.ejs%>
    <%include partials/carousel.ejs%>
    <div id="tab">
    <%include partials/tabular_news.ejs%>
    </div>
  

    <%include partials/footer.ejs%>
</body>
<%include partials/materialize.ejs%>

<script>
     url=document.URL.split("/")
    document.getElementById('title').innerHTML=String(url[url.length-1].split('%')[0]);
    if(url[url.length-1].split('%')[0]=="News" || url[url.length-1].split('%')[0]=="news"){
            document.getElementById('tab').style.display="none";
    }
</script>
</html>